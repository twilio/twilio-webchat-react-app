name: AWS Secrets Manager
description: Read secrets from AWS Secrets Manager
requirements:
  - docker
  - jq
configuration:
  properties:
    endpoint-url:
      type: string
    env:
      type: object
    file:
      type: array
    json-to-env:
      type: array
    region:
      type: string
  required: []
